name: Unit Tests
on: [push]
jobs:
  unit-tests-run:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Unit Tests run
        run: npm run test:unit
